/**
 * Enum for optional session-specific alerts that instructors can subscribe to
 */
export enum OptionalAlertType {
  QUESTION_FAILURE_RATE = 'question_failure_rate',
  STUDENT_INACTIVITY = 'student_inactivity',
  LOW_PARTICIPATION = 'low_participation',
}
